version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
  nginx:
    build: 
      context: ./nginx-proxy
      dockerfile: Dockerfile
  test:
    build:
      context: ./tests
      dockerfile: Dockerfile